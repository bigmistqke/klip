{
  "name": "klip-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @klip/app dev",
    "build": "pnpm --filter @klip/app build",
    "preview": "pnpm --filter @klip/app preview",
    "test": "pnpm -r test",
    "test:run": "pnpm -r test:run",
    "test:browser": "pnpm --filter @klip/codecs test:browser",
    "test:browser:run": "pnpm --filter @klip/codecs test:browser:run"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
