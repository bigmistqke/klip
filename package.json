{
  "name": "eddy-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @eddy/app dev",
    "build": "pnpm -r build",
    "preview": "pnpm --filter @eddy/app preview",
    "test": "pnpm -r test",
    "test:run": "pnpm -r test:run",
    "test:browser": "pnpm --filter @eddy/codecs test:browser",
    "test:browser:run": "pnpm --filter @eddy/codecs test:browser:run",
    "perf": "npx tsx scripts/perf-test.ts",
    "types": "pnpm -r types"
  },
  "devDependencies": {
    "@types/audioworklet": "^0.0.92",
    "puppeteer": "^24.34.0",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@bigmistqke/rpc": "^0.1.2"
  }
}
